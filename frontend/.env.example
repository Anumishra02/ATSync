# Backend API base URL. Optional in dev — the app falls back to the
# current Render host if this is unset. Set it in Vercel's project
# settings for production / preview builds.
VITE_API_URL=https://atsync-5l6k.onrender.com/api/resume
